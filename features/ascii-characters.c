#include <stdio.h>

int main() {

  // * You can assign ASCII values to a char variable
  char a = 65;

  printf("%c\n", a); // A

  return 0;
}

// TIP: For a list of ascii characters, @see:
// https://www.w3schools.com/charsets/ref_html_ascii.asp
