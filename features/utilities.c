#include "utilities.h"
#include <stdio.h>

// * Function definitions
void hello() { printf("Hello World \n"); }

int sum(int x, int y) {
  int result = x + y;
  return result;
}

// TIP: This file is being included in functions_headers.c
