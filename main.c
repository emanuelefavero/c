#include <stdio.h>

int main() {

  int count = 1;

  do {
    printf("%d \n", count);
    count++;
  } while (count <= 0);

  return 0;
}

// TIP: Use `cpace main.c` to run the code with auto compile
